"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.base = base;
exports.name = name;
exports.ext = ext;
exports.dir = dir;
exports.join = join;
exports.normalize = normalize;

var _core = require("@dogmalang/core");

const path = _core.dogma.use(require("path"));

function base(...args) {
  {
    return path.parse(path.join(...args)).base;
  }
}

function name(...args) {
  {
    return path.parse(path.join(...args)).name;
  }
}

function ext(...args) {
  {
    return path.parse(path.join(...args)).ext;
  }
}

function dir(...args) {
  {
    return path.parse(path.join(...args)).dir;
  }
}

function join(...args) {
  {
    return path.join(...args);
  }
}

function normalize(...args) {
  {
    return path.normalize(path.join(...args));
  }
}