"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.ms = ms;

//Get miliseconds from other unit.
function ms(time) {
  const PAT = /^([0-9]+) *(ms|s|m|h)?$/; //(1) get time

  if (time == null) {
    time = 0;
  } else if (typeof time == "string" && PAT.test(time)) {
    time = PAT.exec(time);

    switch (time[2]) {
      case "s":
        time = time[1] * 1000;
        break;

      case "m":
        time = time[1] * 1000 * 60;
        break;

      case "h":
        time = time[1] * 1000 * 60 * 60;
        break;

      default:
        time = time[1] * 1;
    }
  } //(2) return


  return time;
}